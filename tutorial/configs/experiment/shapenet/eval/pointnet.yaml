# @package _global_
defaults:
  - override /model: pointnet 
  - override /tracker: shapenet_car_development_tracker
  - override /pipeline: shapenet_pipeline

name: shapenet-car-pointnet-moment-pos-normalization-eval
trainer:
  max_epochs: 0
  precision: float16

model:
  is_frozen: true
  hidden_dim: 64
  initializers:
    - kind: ksuit.initializers.PreviousRunInitializer
      model_info: ema=0.9999
      checkpoint: latest
      model_name: pointnet
      run_id: 2025-11-01_8zlau 