# @package _global_
defaults:
  - override /model: pointnet 
  - override /tracker: shapenet_car_development_tracker
  - override /pipeline: shapenet_pipeline

name: shapenet-car-pointnet-moment-pos-normalization
trainer:
  precision: float16 

resume_run_id: dlby8au0 # only the stage id is needed to resume

model:
  hidden_dim: 64
  position_projection: "linear"




