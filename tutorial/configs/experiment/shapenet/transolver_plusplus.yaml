# @package _global_
defaults:
  - override /model: transolver  
  - override /tracker: shapenet_car_development_tracker 

name: shapenet-car-transolver-plusplus

model:
  name: transolver_plusplus
  attention_arguments:
    num_slices: 512

